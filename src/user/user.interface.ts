export interface IGithubUserTypes {
  githubId: string;
  avatar: string;
  name: string;
  description: string;
  location: string;
  githubAccessToken: string;
  isMember: boolean;
  memoirAccessToken?: string;
}
